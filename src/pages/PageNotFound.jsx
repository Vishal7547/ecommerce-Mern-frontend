import React from "react";
import Layout from "../components/layout/Layout";

const PageNotFound = () => {
  return <Layout title="page not found go back">PageNotFound</Layout>;
};

export default PageNotFound;
